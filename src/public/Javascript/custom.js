$( document ).ready( ()=> {
	$("#NextCard").click( function() {
		console.log("clicked nextcard button")
		// $.post("/SwitchCard", {donation: parseFloat(Donatie).toFixed(2), kilometer: km }, (res)=>{
		// })
	})
})